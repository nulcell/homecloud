{{- if .Values.radarr.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: radarr
  namespace: {{ include "mediaStack.namespace" . }}
  labels:
    {{- include "mediaStack.labels" (dict "ctx" . "app" "radarr") | nindent 4 }}
spec:
  type: {{ .Values.radarr.service.type }}
  selector:
    {{- include "mediaStack.selectorLabels" (dict "ctx" . "app" "radarr") | nindent 4 }}
  ports:
    - name: web
      port: {{ .Values.radarr.service.ports.web }}
      targetPort: {{ .Values.radarr.service.ports.web }}
      protocol: TCP
{{- end }}
