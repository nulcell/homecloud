apiVersion: apps/v1
kind: Deployment
metadata:
  name: n8n
  namespace: {{ include "n8n.namespace" . }}
  labels:
    {{- include "n8n.labels" . | nindent 4 }}
  {{- with .Values.commonAnnotations }}
  annotations:
    traefik.ingress.kubernetes.io/router.tls.certresolver: {{ .Values.ingress.certIssuer }}
    traefik.ingress.kubernetes.io/ssl-host: "{{ .Values.ingress.sslHost }}"
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  replicas: {{ .Values.n8n.replicaCount }}
  selector:
    matchLabels:
      {{- include "n8n.selectorLabels" . | nindent 6 }}
  {{- if .Values.n8n.updateStrategy }}
  strategy:
    {{- toYaml .Values.n8n.updateStrategy | nindent 4 }}
  {{- end }}
  template:
    metadata:
      annotations:
        traefik.ingress.kubernetes.io/router.tls.certresolver: {{ .Values.ingress.certIssuer }}
        traefik.ingress.kubernetes.io/ssl-host: "{{ .Values.ingress.sslHost }}"
      {{- with .Values.n8n.podAnnotations }}
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "n8n.selectorLabels" . | nindent 8 }}
        {{- with .Values.n8n.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if .Values.hostNetwork }}
      hostNetwork: {{ .Values.hostNetwork }}
      {{- end }}
      {{- if .Values.dnsPolicy }}
      dnsPolicy: {{ .Values.dnsPolicy }}
      {{- end }}
      containers:
        - env:
            - name: DB_TYPE
              value: sqlite
            - name: GENERIC_TIMEZONE
              value: Europe/Amsterdam
            - name: N8N_ENFORCE_SETTINGS_FILE_PERMISSIONS
              value: "true"
            - name: N8N_HOST
              value: n8n.nulcell.com
            - name: N8N_LOG_LEVEL
              value: info
            - name: N8N_NATIVE_PYTHON_RUNNER
              value: "true"
            - name: N8N_PORT
              value: "5678"
            - name: N8N_PROTOCOL
              value: https
            - name: N8N_PROXY_HOPS
              value: "1"
            - name: N8N_RUNNERS_AUTH_TOKEN
              value: your-secret-here
            - name: N8N_RUNNERS_BROKER_LISTEN_ADDRESS
              value: 0.0.0.0
            - name: N8N_RUNNERS_ENABLED
              value: "true"
            - name: N8N_RUNNERS_EXTERNAL_ALLOW
              value: '*'
            - name: N8N_RUNNERS_MODE
              value: external
            - name: N8N_RUNNERS_STDLIB_ALLOW
              value: '*'
            - name: NODE_ENV
              value: production
            - name: NODE_FUNCTION_ALLOW_BUILTIN
              value: '*'
            - name: NODE_FUNCTION_ALLOW_EXTERNAL
              value: '*'
            - name: TZ
              value: Europe/Amsterdam
            - name: WEBHOOK_URL
              value: https://{{ .Values.ingress.host | default "localhost" }}/
          image: "{{ .Values.n8n.image.repository }}:{{ .Values.n8n.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.n8n.image.pullPolicy }}
          name: n8n
          ports:
            - containerPort: 5678
              name: web-port
              protocol: TCP
            - containerPort: 5679
              name: broker-port
              protocol: TCP
          resources:
            {{- toYaml .Values.n8n.resources | nindent 12 }}
          securityContext:
            {{- toYaml .Values.n8n.containerSecurityContext | nindent 12 }}
          volumeMounts:
            - mountPath: /home/node/.n8n
              name: n8n-data
            - mountPath: /files
              name: n8n-files
      {{- if .Values.priorityClassName }}
      priorityClassName: {{ .Values.priorityClassName }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.n8n.podSecurityContext | nindent 8 }}
      restartPolicy: Always
      volumes:
        - name: n8n-data
          persistentVolumeClaim:
            claimName: n8n-data
        - name: n8n-files
          persistentVolumeClaim:
            claimName: n8n-files
