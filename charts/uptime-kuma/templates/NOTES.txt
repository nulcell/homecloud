Uptime Kuma has been deployed.

{{- if .Values.gateway.enabled }}
Access the UI at: https://{{ .Values.gateway.hostname }}
{{- else }}
Gateway routing is disabled. Configure your own HTTPRoute or cloudflared tunnel to expose the service.

Internal service: {{ .Values.service.name }}.{{ include "uptime-kuma.namespace" . }}.svc.cluster.local:{{ .Values.service.port }}
{{- end }}

Data is persisted at /app/data inside the container (PVC: uptime-kuma-data).

Note: ICMP ping monitors require NET_RAW capability. Add it under
containerSecurityContext.capabilities.add if needed.
