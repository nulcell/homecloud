{{- if .Values.jellyseerr.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: jellyseerr
  namespace: {{ include "mediaStack.namespace" . }}
  labels:
    {{- include "mediaStack.labels" (dict "ctx" . "app" "jellyseerr") | nindent 4 }}
spec:
  type: {{ .Values.jellyseerr.service.type }}
  selector:
    {{- include "mediaStack.selectorLabels" (dict "ctx" . "app" "jellyseerr") | nindent 4 }}
  ports:
    - name: web
      port: {{ .Values.jellyseerr.service.ports.web }}
      targetPort: {{ .Values.jellyseerr.service.ports.web }}
      protocol: TCP
{{- end }}
