# Placeholder — encrypt before committing real values:
#   sops --encrypt --in-place gitops/infrastructure/sops-secrets/example-secret.enc.yaml
#
# `.sops.yaml` at the repo root selects the age public key and limits encryption
# to data/stringData, leaving the rest readable in diffs.
apiVersion: v1
kind: Secret
metadata:
  name: example
  namespace: default
type: Opaque
stringData:
  username: admin
  password: REPLACE_ME
