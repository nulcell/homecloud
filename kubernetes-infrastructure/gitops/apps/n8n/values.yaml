# Overrides for the n8n chart at kubernetes-infrastructure/charts/n8n/.
# Everything not listed here keeps the chart's default value.

n8n:
  timezone: Europe/Amsterdam

gateway:
  enabled: true
  hostname: n8n.nulcell.com

# Reference to the SOPS-encrypted Secret in this folder (n8n-runner-auth.enc.yaml).
taskRunner:
  authToken:
    secretName: n8n-runner-auth
    secretKey: auth-token

persistence:
  storageClass: longhorn
  accessMode: ReadWriteOnce # flip to ReadWriteMany once Longhorn RWX is desired
  size: 5Gi
