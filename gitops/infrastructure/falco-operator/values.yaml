# falco-operator 0.1.0 (app v0.2.2) — Falco Operator controller.
# Watches all namespaces by default; reconciles Falco, Component, Plugin,
# Rulesfile, Config CRs from gitops/security/*.

replicaCount: 1

resources:
  requests:
    cpu: 20m
    memory: 64Mi
  limits:
    cpu: 200m
    memory: 256Mi
