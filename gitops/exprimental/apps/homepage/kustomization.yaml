apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: homepage

helmGlobals:
  chartHome: ../../../charts

helmCharts:
  - name: homepage
    releaseName: homepage
    namespace: homepage
    valuesFile: values.yaml
    includeCRDs: false

resources:
  - namespace.yaml
