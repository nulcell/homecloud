apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: falco

helmCharts:
  - name: falco-talon
    repo: https://falcosecurity.github.io/charts
    version: 0.4.0
    releaseName: falco-talon
    namespace: falco
    valuesFile: values.yaml
    includeCRDs: true
