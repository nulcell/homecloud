# Overrides for charts/falco-stack/. Everything else uses chart defaults.

falcosidekickUi:
  gateway:
    hostname: falco.nulcell.com

# Flip on after confirming real Service ports + selectors in the live cluster:
#   kubectl -n falco get svc -o yaml
#   kubectl -n falco get pod --show-labels
# monitoring:
#   serviceMonitors:
#     enabled: true
