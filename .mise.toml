# .mise.toml — Tool version management for homecloud
# Run `mise install` to install all pinned tools.
# Run `mise use <tool>@<version>` to update a pinned version.
# See https://mise.jdx.dev

[tools]

# ── Kubernetes ───────────────────────────────────────────────────────────────
kubectl  = "1.36.1"
helm     = "4.2.0"
argocd   = "3.4.5"

# ── Talos Linux ──────────────────────────────────────────────────────────────
# talosctl manages Talos Linux nodes and clusters.
# talhelper generates Talos machine configs from a single source of truth.
talosctl  = "1.13.2"
talhelper = "3.1.10"

# ── Networking ───────────────────────────────────────────────────────────────
# cilium-cli: debug Cilium / Gateway API / L2 announcements.
# tailscale CLI: interact with the tailnet during operator work.
cilium-cli                 = "0.19.2"
"aqua:tailscale/tailscale" = "1.96.2"

# ── Data processing ──────────────────────────────────────────────────────────
yq   = "4.53.2"
jq   = "1.8.1"

# ── Secrets ──────────────────────────────────────────────────────────────────
# SOPS encrypts manifests in-repo; age provides the key material.
# 1Password CLI is installed system-wide via Homebrew (not managed by mise).
sops = "3.13.0"
age  = "1.3.1"

# ── Language runtimes ────────────────────────────────────────────────────────
python = "3.13"

# ── Local testing ────────────────────────────────────────────────────────────
kind = "0.31.0"

[settings]
# Automatically activate tool versions when entering directories.
experimental = true
